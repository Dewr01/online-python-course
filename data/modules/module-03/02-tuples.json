{
    "tasks": [
        {
            "answer": "(1,2,3)",
            "hint": "Кортеж создается круглыми скобками.",
            "id": "m3t5",
            "question": "Как создать кортеж с элементами 1, 2, 3?"
        },
        {
            "answer": "нет",
            "hint": "Кортежи неизменяемые (immutable).",
            "id": "m3t6",
            "question": "Можно ли изменить кортеж после создания? (да/нет)"
        },
        {
            "answer": "1",
            "hint": "Индексация работает как в списках.",
            "id": "m3t7",
            "question": "Что получится при (1,2,3)[0]?"
        },
        {
            "answer": "(1,)",
            "hint": "Нужна запятая после элемента: (1,) не (1).",
            "id": "m3t8",
            "question": "Как создать кортеж из одного элемента?"
        }
    ],
    "theory": "**Кортежи (tuple)** — упорядоченные неизменяемые коллекции элементов. Главное отличие от списков — кортежи нельзя изменить после создания (immutable).\n📝 **Создание кортежей**:\n```python\n# Круглые скобки\ncoordinates = (10, 20)\ncolors = (\"красный\", \"зеленый\", \"синий\")\n\n# Без скобок (запятая создает кортеж)\npoint = 10, 20, 30\nsingle = (42,)  # Один элемент (запятая обязательна!)\n\n# Пустой кортеж\nempty = ()\n```\n\n🔢 **Индексация и срезы** (как в списках):\n```python\ndata = (1, 2, 3, 4, 5)\nprint(data[0])    # 1 (первый элемент)\nprint(data[-1])   # 5 (последний элемент)\nprint(data[1:4])  # (2, 3, 4) (срез)\nprint(data[:3])   # (1, 2, 3) (первые 3)\n```\n\n❌ **Что НЕЛЬЗЯ делать с кортежами**:\n```python\ncoordinates = (10, 20)\n# coordinates[0] = 15  # ОШИБКА! Нельзя изменять\n# coordinates.append(30)  # ОШИБКА! Нет метода append\n# coordinates.remove(10)  # ОШИБКА! Нет метода remove\n```\n\n✅ **Что МОЖНО делать с кортежами**:\n```python\npoint = (10, 20, 30)\nprint(len(point))        # 3 (длина)\nprint(point.count(10))   # 1 (количество вхождений)\nprint(point.index(20))   # 1 (индекс элемента)\nprint(max(point))        # 30 (максимум)\nprint(min(point))        # 10 (минимум)\n```\n\n💡 **Практические применения**:\n\n**Координаты точки**:\n```python\nx, y = (10, 20)  # Распаковка кортежа\nprint(f\"X: {x}, Y: {y}\")\n```\n\n**Возврат нескольких значений из функции**:\n```python\ndef get_name_age():\n    return \"Анна\", 25\n\nname, age = get_name_age()  # Распаковка\nprint(f\"Имя: {name}, Возраст: {age}\")\n```\n\n**Группировка связанных данных**:\n```python\nstudent = (\"Иван\", 20, \"Информатика\")\nname, age, major = student\nprint(f\"{name}, {age} лет, изучает {major}\")\n```\n\n**Сравнение кортежей**:\n```python\npoint1 = (1, 2)\npoint2 = (1, 3)\nprint(point1 < point2)  # True (сравнение по элементам)\n```\n\n🔍 **Когда использовать кортежи**:\n- Координаты, размеры, настройки\n- Возврат нескольких значений из функции\n- Ключи в словарях (только неизменяемые типы)\n- Когда нужно гарантировать неизменность данных",
    "title": "Кортежи"
}
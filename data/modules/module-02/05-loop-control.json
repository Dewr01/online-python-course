{
    "tasks": [
        {
            "answer": "прерывает цикл",
            "hint": "break немедленно выходит из цикла.",
            "id": "m2t17",
            "question": "Что делает break в цикле?"
        },
        {
            "answer": "пропускает итерацию",
            "hint": "continue переходит к следующей итерации.",
            "id": "m2t18",
            "question": "Что делает continue в цикле?"
        },
        {
            "answer": "0\n2",
            "hint": "continue пропускает печать при i==1.",
            "id": "m2t19",
            "question": "Что выведет: for i in range(3): if i==1: continue; print(i)?"
        },
        {
            "answer": "когда цикл завершился без break",
            "hint": "else выполняется при нормальном завершении цикла.",
            "id": "m2t20",
            "question": "Когда выполняется else в цикло for?"
        }
    ],
    "theory": "**Управление циклами** позволяет гибко контролировать выполнение повторяющихся операций. Python предоставляет ключевые слова `break`, `continue` и `else` для управления потоком выполнения.\n🛑 **break** — полное прерывание цикла:\n```python\nfor i in range(10):\n    if i == 5:\n        break  # Выходим из цикла при i=5\n    print(i)  # Выведет: 0, 1, 2, 3, 4\n```\n\n⏭️ **continue** — пропуск текущей итерации:\n```python\nfor i in range(5):\n    if i == 2:\n        continue  # Пропускаем i=2\n    print(i)  # Выведет: 0, 1, 3, 4\n```\n\n✅ **else в циклах** — выполняется при нормальном завершении:\n```python\n# Поиск числа в списке\nnumbers = [1, 3, 5, 7, 9]\nfor num in numbers:\n    if num == 4:\n        print(\"Найдено!\")\n        break\nelse:\n    print(\"Не найдено!\")  # Выполнится, если break не сработал\n```\n\n💡 **Практические примеры**:\n\n**Поиск первого четного числа**:\n```python\nnumbers = [1, 3, 5, 8, 9]\nfor num in numbers:\n    if num % 2 == 0:\n        print(f\"Первое четное: {num}\")\n        break\nelse:\n    print(\"Четных чисел нет\")\n```\n\n**Пропуск отрицательных чисел**:\n```python\nnumbers = [1, -2, 3, -4, 5]\nfor num in numbers:\n    if num < 0:\n        continue  # Пропускаем отрицательные\n    print(num)  # Выведет: 1, 3, 5\n```\n\n**Проверка всех элементов**:\n```python\nages = [18, 20, 16, 22]\nfor age in ages:\n    if age < 18:\n        print(\"Есть несовершеннолетние\")\n        break\nelse:\n    print(\"Все совершеннолетние\")\n```\n\n⚠️ **Важно**: `else` в цикле выполняется только если цикл завершился **без** `break`!",
    "title": "Управление циклом"
}
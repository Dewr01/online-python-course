{
    "tasks": [
        {
            "answer": "def",
            "hint": "Объявление начинается с def.",
            "id": "m4t1",
            "question": "Как объявить функцию add(a,b), возвращающую сумму? (короткий ответ: ключевое слово)"
        },
        {
            "answer": "greet()",
            "hint": "Вызов функции: имя_функции().",
            "id": "m4t2",
            "question": "Как вызвать функцию greet()?"
        },
        {
            "answer": "переменная в определении",
            "hint": "Параметр — переменная в скобках при определении функции.",
            "id": "m4t3",
            "question": "Что такое параметр функции?"
        },
        {
            "answer": "значение при вызове",
            "hint": "Аргумент — конкретное значение, передаваемое при вызове.",
            "id": "m4t4",
            "question": "Что такое аргумент функции?"
        }
    ],
    "theory": "**Функции** — это блоки кода, которые можно переиспользовать многократно. Это основа модульного программирования в Python!\n📝 **Синтаксис определения функции**:\n```python\ndef имя_функции(параметры):\n    \"\"\"Документация функции (опционально)\"\"\"\n    # Тело функции\n    return результат  # Опционально\n```\n\n🔧 **Простые примеры**:\n\n**Функция без параметров**:\n```python\ndef greet():\n    print(\"Привет, мир!\")\n\ngreet()  # Вызов функции\n```\n\n**Функция с параметрами**:\n```python\ndef add(a, b):\n    return a + b\n\nresult = add(5, 3)  # result = 8\nprint(add(10, 20))  # 30\n```\n\n**Функция с документацией**:\n```python\ndef calculate_area(length, width):\n    \"\"\"\n    Вычисляет площадь прямоугольника\n    \n    Args:\n        length (float): Длина прямоугольника\n        width (float): Ширина прямоугольника\n    \n    Returns:\n        float: Площадь прямоугольника\n    \"\"\"\n    return length * width\n\narea = calculate_area(5.0, 3.0)  # 15.0\n```\n\n💡 **Практические примеры**:\n\n**Калькулятор оценок**:\n```python\ndef get_grade(score):\n    if score >= 90:\n        return \"A\"\n    elif score >= 80:\n        return \"B\"\n    elif score >= 70:\n        return \"C\"\n    else:\n        return \"F\"\n\nstudent_score = 85\ngrade = get_grade(student_score)\nprint(f\"Оценка: {grade}\")  # Оценка: B\n```\n\n**Проверка четности**:\n```python\ndef is_even(number):\n    return number % 2 == 0\n\nprint(is_even(4))   # True\nprint(is_even(7))   # False\n```\n\n**Форматирование имени**:\n```python\ndef format_name(first_name, last_name):\n    return f\"{first_name.title()} {last_name.title()}\"\n\nfull_name = format_name(\"иван\", \"петров\")\nprint(full_name)  # Иван Петров\n```\n\n🎯 **Преимущества функций**:\n- **Переиспользование кода** — один раз написал, много раз используешь\n- **Читаемость** — код становится более понятным\n- **Тестирование** — легко проверить отдельную функцию\n- **Модульность** — разбиение сложной задачи на части\n- **Отладка** — легче найти ошибки в изолированном коде\n\n⚠️ **Важно помнить**:\n- Функция должна быть определена до её вызова\n- Отступы критически важны для тела функции\n- Функция без `return` возвращает `None`\n- Имена функций должны быть описательными",
    "title": "Определение функций"
}
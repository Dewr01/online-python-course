{
    "tasks": [
        {
            "answer": "return",
            "hint": "Ключевое слово return.",
            "id": "m4t9",
            "question": "Как вернуть значение из функции? (ключевое слово)"
        },
        {
            "answer": "None",
            "hint": "Функция без return возвращает None.",
            "id": "m4t10",
            "question": "Что возвращает функция без return?"
        },
        {
            "answer": "return a, b",
            "hint": "return a, b возвращает кортеж (a, b).",
            "id": "m4t11",
            "question": "Как вернуть два значения из функции?"
        },
        {
            "answer": "функция завершится",
            "hint": "return немедленно завершает выполнение функции.",
            "id": "m4t12",
            "question": "Что произойдет после выполнения return в функции?"
        }
    ],
    "theory": "**Возврат значений** — это способ передачи результата работы функции обратно в вызывающий код. Ключевое слово `return` — основа взаимодействия функций с остальной программой!\n🔄 **Основы возврата значений**:\n\n**Простой возврат**:\n```python\ndef add(a, b):\n    return a + b\n\nresult = add(5, 3)  # result = 8\nprint(result)       # 8\n```\n\n**Возврат без значения**:\n```python\ndef greet(name):\n    print(f\"Привет, {name}!\")\n    # Нет return - функция возвращает None\n\nresult = greet(\"Анна\")  # result = None\nprint(result)           # None\n```\n\n**Явный возврат None**:\n```python\ndef process_data(data):\n    if not data:\n        return None  # Явно возвращаем None\n    # Обработка данных...\n    return \"Обработано\"\n```\n\n🌟 **Возврат нескольких значений**:\n\n**Через кортеж**:\n```python\ndef get_name_age():\n    return \"Анна\", 25  # Возвращает кортеж (\"Анна\", 25)\n\nname, age = get_name_age()  # Распаковка кортежа\nprint(f\"Имя: {name}, Возраст: {age}\")  # Имя: Анна, Возраст: 25\n```\n\n**Возврат списка**:\n```python\ndef get_stats(numbers):\n    return [\n        min(numbers),  # Минимум\n        max(numbers),  # Максимум\n        sum(numbers) / len(numbers)  # Среднее\n    ]\n\ndata = [1, 5, 3, 9, 2]\nmin_val, max_val, avg_val = get_stats(data)\nprint(f\"Мин: {min_val}, Макс: {max_val}, Среднее: {avg_val}\")\n```\n\n**Возврат словаря**:\n```python\ndef analyze_text(text):\n    return {\n        \"length\": len(text),\n        \"words\": len(text.split()),\n        \"uppercase\": sum(1 for c in text if c.isupper()),\n        \"lowercase\": sum(1 for c in text if c.islower())\n    }\n\nstats = analyze_text(\"Hello World!\")\nprint(stats)  # {'length': 12, 'words': 2, 'uppercase': 2, 'lowercase': 8}\n```\n\n🛑 **Ранний возврат (Early Return)**:\n```python\ndef divide(a, b):\n    if b == 0:\n        return \"Ошибка: деление на ноль!\"  # Ранний возврат\n    \n    return a / b  # Этот код выполнится только если b != 0\n\nprint(divide(10, 2))  # 5.0\nprint(divide(10, 0))  # Ошибка: деление на ноль!\n```\n\n**Проверка валидности данных**:\n```python\ndef get_user_info(user_id):\n    if not isinstance(user_id, int):\n        return None  # Неверный тип данных\n    \n    if user_id <= 0:\n        return None  # Неверный ID\n    \n    # Получение данных пользователя...\n    return {\"id\": user_id, \"name\": \"Пользователь\"}\n```\n\n💡 **Практические примеры**:\n\n**Калькулятор с историей**:\n```python\ndef calculate_with_history(operation, a, b):\n    if operation == \"add\":\n        result = a + b\n    elif operation == \"subtract\":\n        result = a - b\n    elif operation == \"multiply\":\n        result = a * b\n    elif operation == \"divide\":\n        if b == 0:\n            return None, \"Деление на ноль\"\n        result = a / b\n    else:\n        return None, \"Неизвестная операция\"\n    \n    return result, f\"{a} {operation} {b} = {result}\"\n\nresult, history = calculate_with_history(\"add\", 5, 3)\nprint(f\"Результат: {result}\")  # Результат: 8\nprint(f\"История: {history}\")   # История: 5 add 3 = 8\n```\n\n**Поиск в списке**:\n```python\ndef find_student(students, name):\n    for i, student in enumerate(students):\n        if student[\"name\"] == name:\n            return i, student  # Возвращаем индекс и данные\n    \n    return None, None  # Не найден\n\nstudents = [\n    {\"name\": \"Анна\", \"grade\": 85},\n    {\"name\": \"Борис\", \"grade\": 92},\n    {\"name\": \"Виктор\", \"grade\": 78}\n]\n\nindex, student = find_student(students, \"Борис\")\nif index is not None:\n    print(f\"Найден: {student['name']}, оценка: {student['grade']}\")\nelse:\n    print(\"Студент не найден\")\n```\n\n**Функция с множественными возвратами**:\n```python\ndef process_number(num):\n    if num < 0:\n        return \"отрицательное\", abs(num)\n    elif num == 0:\n        return \"ноль\", 0\n    elif num % 2 == 0:\n        return \"четное положительное\", num\n    else:\n        return \"нечетное положительное\", num\n\ntype_name, value = process_number(-5)\nprint(f\"Тип: {type_name}, значение: {value}\")  # Тип: отрицательное, значение: 5\n```\n\n⚠️ **Важные моменты**:\n- `return` **немедленно** завершает выполнение функции\n- Код после `return` никогда не выполнится\n- Функция без `return` возвращает `None`\n- Можно возвращать любые типы данных\n- Возврат нескольких значений создает кортеж\n- Используйте ранний возврат для упрощения логики",
    "title": "Возврат значений"
}
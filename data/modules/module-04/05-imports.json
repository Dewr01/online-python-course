{
    "tasks": [
        {
            "answer": "import math",
            "hint": "import имя_модуля",
            "id": "m4t17",
            "question": "Как импортировать модуль math?"
        },
        {
            "answer": "from math import sqrt",
            "hint": "from модуль import функция",
            "id": "m4t18",
            "question": "Как импортировать только функцию sqrt из math?"
        },
        {
            "answer": "import math as m",
            "hint": "import модуль as псевдоним",
            "id": "m4t19",
            "question": "Как импортировать math с псевдонимом m?"
        },
        {
            "answer": "datetime",
            "hint": "Модуль datetime для работы с датами и временем.",
            "id": "m4t20",
            "question": "Какой модуль содержит функции для работы с датами?"
        }
    ],
    "theory": "**Импорт модулей** — это способ использования готового кода из других файлов или стандартной библиотеки Python. Это основа модульного программирования и переиспользования кода!\n📦 **Способы импорта**:\n\n**1. Импорт всего модуля**:\n```python\nimport math\nimport random\nimport datetime\n\n# Использование с префиксом модуля\nprint(math.sqrt(16))        # 4.0\nprint(random.randint(1, 10)) # Случайное число от 1 до 10\nprint(datetime.datetime.now()) # Текущая дата и время\n```\n\n**2. Импорт конкретных функций**:\n```python\nfrom math import sqrt, pi, sin\nfrom random import choice, shuffle\nfrom datetime import date\n\n# Использование без префикса\nprint(sqrt(25))              # 5.0\nprint(pi)                    # 3.141592653589793\nprint(choice(['a', 'b', 'c'])) # Случайный элемент\nprint(date.today())          # Текущая дата\n```\n\n**3. Импорт с псевдонимом**:\n```python\nimport math as m\nimport datetime as dt\nimport random as r\n\nprint(m.sqrt(36))            # 6.0\nprint(dt.datetime.now())     # Текущая дата и время\nprint(r.random())            # Случайное число от 0 до 1\n```\n\n**4. Импорт всего содержимого (не рекомендуется)**:\n```python\nfrom math import *  # Импортирует все функции\n\nprint(sqrt(49))     # 7.0 (без префикса math.)\nprint(pi)           # 3.141592653589793\n```\n\n🔧 **Популярные модули стандартной библиотеки**:\n\n**math — математические функции**:\n```python\nimport math\n\nprint(math.sqrt(16))        # 4.0\nprint(math.pow(2, 3))       # 8.0\nprint(math.sin(math.pi/2))  # 1.0\nprint(math.ceil(4.2))       # 5 (округление вверх)\nprint(math.floor(4.8))      # 4 (округление вниз)\nprint(math.factorial(5))    # 120\n```\n\n**random — генерация случайных чисел**:\n```python\nimport random\n\nprint(random.random())           # Случайное число 0.0-1.0\nprint(random.randint(1, 10))     # Случайное целое 1-10\nprint(random.choice(['a', 'b', 'c'])) # Случайный элемент\n\n# Перемешивание списка\ncards = ['A', 'K', 'Q', 'J']\nrandom.shuffle(cards)\nprint(cards)  # ['Q', 'A', 'J', 'K'] (случайный порядок)\n```\n\n**datetime — работа с датами и временем**:\n```python\nfrom datetime import datetime, date, timedelta\n\n# Текущая дата и время\nnow = datetime.now()\nprint(now)  # 2024-01-15 14:30:25.123456\n\n# Только дата\ntoday = date.today()\nprint(today)  # 2024-01-15\n\n# Создание конкретной даты\nbirthday = date(1990, 5, 15)\nprint(birthday)  # 1990-05-15\n\n# Арифметика с датами\nage = today - birthday\nprint(age.days)  # Количество дней\n\n# Добавление времени\ntomorrow = today + timedelta(days=1)\nprint(tomorrow)  # 2024-01-16\n```\n\n**os — работа с операционной системой**:\n```python\nimport os\n\n# Текущая директория\nprint(os.getcwd())  # /path/to/current/directory\n\n# Список файлов в директории\nfiles = os.listdir('.')\nprint(files)  # ['file1.py', 'file2.txt', ...]\n\n# Проверка существования файла\nif os.path.exists('myfile.txt'):\n    print('Файл существует')\n\n# Создание директории\nos.makedirs('new_folder', exist_ok=True)\n```\n\n**json — работа с JSON**:\n```python\nimport json\n\n# Словарь в JSON\ndata = {'name': 'Анна', 'age': 25, 'city': 'Москва'}\njson_string = json.dumps(data, ensure_ascii=False)\nprint(json_string)  # {\"name\": \"Анна\", \"age\": 25, \"city\": \"Москва\"}\n\n# JSON в словарь\nparsed_data = json.loads(json_string)\nprint(parsed_data['name'])  # Анна\n\n# Сохранение в файл\nwith open('data.json', 'w', encoding='utf-8') as f:\n    json.dump(data, f, ensure_ascii=False)\n\n# Загрузка из файла\nwith open('data.json', 'r', encoding='utf-8') as f:\n    loaded_data = json.load(f)\n```\n\n💡 **Практические примеры**:\n\n**Генератор паролей**:\n```python\nimport random\nimport string\n\ndef generate_password(length=8):\n    characters = string.ascii_letters + string.digits + '!@#$%'\n    password = ''.join(random.choice(characters) for _ in range(length))\n    return password\n\nprint(generate_password(12))  # Случайный пароль из 12 символов\n```\n\n**Калькулятор времени**:\n```python\nfrom datetime import datetime, timedelta\n\ndef time_until_birthday(birth_month, birth_day):\n    today = datetime.now()\n    this_year_birthday = datetime(today.year, birth_month, birth_day)\n    \n    if this_year_birthday < today:\n        this_year_birthday = datetime(today.year + 1, birth_month, birth_day)\n    \n    days_until = (this_year_birthday - today).days\n    return days_until\n\nprint(f\"До дня рождения: {time_until_birthday(5, 15)} дней\")\n```\n\n**Анализ файлов**:\n```python\nimport os\nimport json\nfrom datetime import datetime\n\ndef analyze_directory(path):\n    files_info = []\n    \n    for filename in os.listdir(path):\n        file_path = os.path.join(path, filename)\n        if os.path.isfile(file_path):\n            stat = os.stat(file_path)\n            files_info.append({\n                'name': filename,\n                'size': stat.st_size,\n                'modified': datetime.fromtimestamp(stat.st_mtime)\n            })\n    \n    return files_info\n\n# Анализ текущей директории\nfiles = analyze_directory('.')\nfor file_info in files:\n    print(f\"{file_info['name']}: {file_info['size']} байт\")\n```\n\n⚠️ **Лучшие практики импорта**:\n- **Импортируйте только то, что нужно** — `from math import sqrt` лучше чем `import math`\n- **Используйте псевдонимы для длинных имен** — `import numpy as np`\n- **Группируйте импорты** — стандартная библиотека, сторонние модули, локальные модули\n- **Избегайте `from module import *`** — может привести к конфликтам имен\n- **Импорты в начале файла** — для лучшей читаемости",
    "title": "Импорт модулей"
}
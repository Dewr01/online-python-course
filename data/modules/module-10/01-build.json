{
    "title": "Сборка мини-приложения",
    "theory": "**Итоговый проект: Веб-приложение на Pyodide** — демонстрация всех изученных навыков Python.\n## Цель проекта\n\nСоздать полнофункциональное веб-приложение, объединяющее:\n- Все изученные концепции Python\n- Современные практики разработки\n- Пользовательский интерфейс\n- Обработку данных\n- Тестирование и документацию\n\n## Архитектура проекта\n\n**Структура файлов:**\n```\nfinal_project/\n├── index.html              # Главная страница\n├── main.py                 # Основная логика\n├── app/\n│   ├── models.py           # Модели данных\n│   ├── services.py         # Бизнес-логика\n│   └── utils.py            # Вспомогательные функции\n├── tests/\n│   ├── test_models.py\n│   └── test_services.py\n├── static/\n│   ├── style.css\n│   └── script.js\n└── README.md\n```\n\n## Основное приложение\n\n**Главный модуль (main.py):**\n```python\nimport json\nfrom datetime import datetime\nfrom typing import List, Dict, Optional\n\nclass TaskManager:\n    \"\"\"Менеджер задач с полным функционалом\"\"\"\n    \n    def __init__(self):\n        self.tasks: List[Dict] = []\n        self.categories = [\"Работа\", \"Личное\", \"Учеба\", \"Здоровье\"]\n        self.load_data()\n    \n    def add_task(self, title: str, description: str = \"\", \n                 category: str = \"Личное\", priority: int = 1) -> Dict:\n        \"\"\"Добавление новой задачи\"\"\"\n        if not title.strip():\n            raise ValueError(\"Название задачи не может быть пустым\")\n        \n        if priority not in range(1, 6):\n            raise ValueError(\"Приоритет должен быть от 1 до 5\")\n        \n        task = {\n            \"id\": self._generate_id(),\n            \"title\": title.strip(),\n            \"description\": description.strip(),\n            \"category\": category,\n            \"priority\": priority,\n            \"completed\": False,\n            \"created_at\": datetime.now().isoformat(),\n            \"completed_at\": None\n        }\n        \n        self.tasks.append(task)\n        self.save_data()\n        return task\n    \n    def get_tasks(self, category: Optional[str] = None, \n                  completed: Optional[bool] = None) -> List[Dict]:\n        \"\"\"Получение задач с фильтрацией\"\"\"\n        filtered_tasks = self.tasks\n        \n        if category is not None:\n            filtered_tasks = [t for t in filtered_tasks if t[\"category\"] == category]\n        \n        if completed is not None:\n            filtered_tasks = [t for t in filtered_tasks if t[\"completed\"] == completed]\n        \n        return sorted(filtered_tasks, key=lambda x: (-x[\"priority\"], x[\"created_at\"]))\n    \n    def complete_task(self, task_id: str) -> bool:\n        \"\"\"Отметка задачи как выполненной\"\"\"\n        for task in self.tasks:\n            if task[\"id\"] == task_id and not task[\"completed\"]:\n                task[\"completed\"] = True\n                task[\"completed_at\"] = datetime.now().isoformat()\n                self.save_data()\n                return True\n        return False\n    \n    def get_statistics(self) -> Dict:\n        \"\"\"Получение статистики\"\"\"\n        total = len(self.tasks)\n        completed = sum(1 for task in self.tasks if task[\"completed\"])\n        \n        return {\n            \"total_tasks\": total,\n            \"completed_tasks\": completed,\n            \"completion_rate\": (completed / total * 100) if total > 0 else 0\n        }\n```\n\n## Веб-интерфейс\n\n**HTML структура:**\n```html\n<!DOCTYPE html>\n<html lang=\"ru\">\n<head>\n    <meta charset=\"UTF-8\">\n    <title>Task Manager - Python Pyodide</title>\n    <script src=\"https://cdn.jsdelivr.net/pyodide/v0.24.1/full/pyodide.js\"></script>\n</head>\n<body>\n    <div class=\"container\">\n        <header>\n            <h1>📋 Task Manager</h1>\n        </header>\n        \n        <main>\n            <div class=\"controls\">\n                <button id=\"addTaskBtn\">+ Добавить задачу</button>\n                <button id=\"showStatsBtn\">📊 Статистика</button>\n            </div>\n            \n            <div id=\"tasksList\" class=\"tasks-list\">\n                <!-- Задачи загружаются здесь -->\n            </div>\n        </main>\n    </div>\n    \n    <script src=\"static/script.js\"></script>\n</body>\n</html>\n```\n\n## JavaScript интеграция\n\n**Связь с Python:**\n```javascript\nlet pyodide;\nlet taskManager;\n\nasync function initPyodide() {\n    pyodide = await loadPyodide();\n    \n    await pyodide.runPythonAsync(`\n        from main import TaskManager\n        task_manager = TaskManager()\n    `);\n    \n    taskManager = pyodide.globals.get('task_manager');\n    loadTasks();\n}\n\nasync function addTask(title, description, category, priority) {\n    await pyodide.runPythonAsync(`\n        task_manager.add_task(\n            title=\"${title}\",\n            description=\"${description}\",\n            category=\"${category}\",\n            priority=${priority}\n        )\n    `);\n    loadTasks();\n}\n\nasync function loadTasks() {\n    const tasks = await pyodide.runPythonAsync('task_manager.get_tasks()');\n    displayTasks(tasks);\n}\n\ndocument.addEventListener('DOMContentLoaded', initPyodide);\n```\n\n## Тестирование\n\n**Unit тесты:**\n```python\nimport unittest\nfrom main import TaskManager\n\nclass TestTaskManager(unittest.TestCase):\n    def setUp(self):\n        self.manager = TaskManager()\n        self.manager.tasks = []\n    \n    def test_add_task_success(self):\n        task = self.manager.add_task(\"Test task\", \"Description\", \"Работа\", 3)\n        \n        self.assertEqual(task[\"title\"], \"Test task\")\n        self.assertEqual(len(self.manager.tasks), 1)\n    \n    def test_complete_task(self):\n        task = self.manager.add_task(\"Test task\")\n        result = self.manager.complete_task(task[\"id\"])\n        \n        self.assertTrue(result)\n        self.assertTrue(self.manager.tasks[0][\"completed\"])\n\nif __name__ == '__main__':\n    unittest.main()\n```\n\n## Документация\n\n**README.md:**\n```markdown\n# Task Manager - Python Pyodide\n\nВеб-приложение для управления задачами на Python + Pyodide.\n\n## Особенности\n- ✅ CRUD операции с задачами\n- 📊 Статистика и аналитика\n- 🔍 Поиск и фильтрация\n- 💾 Автосохранение данных\n- 🧪 Полное тестирование\n\n## Технологии\n- Python 3.12\n- Pyodide (Python в браузере)\n- HTML5, CSS3, JavaScript\n- unittest\n\n## Запуск\n1. Откройте `index.html` в браузере\n2. Приложение автоматически загрузит Python\n```\n\n**Ключевые особенности:**\n- Полная интеграция Python и веб-технологий\n- Современная архитектура\n- Комплексное тестирование\n- Профессиональная документация\n- Готовность к демонстрации навыков Junior Python Developer",
    "tasks": [
        {
            "id": "m10t1",
            "question": "Какие основные концепции нужно применить в финальном проекте?",
            "answer": "функции, классы, обработка ошибок",
            "hint": "Все изученные концепции Python."
        },
        {
            "id": "m10t2",
            "question": "На какой платформе создается финальное приложение?",
            "answer": "Pyodide в браузере",
            "hint": "Веб-приложение на Pyodide."
        },
        {
            "id": "m10t3",
            "question": "Какой интерфейс должен иметь финальный проект?",
            "answer": "интерактивный",
            "hint": "Интерактивный пользовательский интерфейс."
        },
        {
            "id": "m10t4",
            "question": "Нужно ли использовать обработку ошибок в финальном проекте? (да/нет)",
            "answer": "да",
            "hint": "Обработка ошибок — важная часть проекта."
        }
    ]
}

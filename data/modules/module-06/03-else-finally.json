{
    "tasks": [
        {
            "answer": "когда ошибки не было",
            "hint": "else выполняется если try завершился без исключений.",
            "id": "m6t9",
            "question": "Когда выполняется else в try/except?"
        },
        {
            "answer": "всегда",
            "hint": "finally выполняется всегда — с ошибкой или без.",
            "id": "m6t10",
            "question": "Когда выполняется finally?"
        },
        {
            "answer": "try, except или else, finally",
            "hint": "try → except (при ошибке) или else (без ошибки) → finally.",
            "id": "m6t11",
            "question": "В каком порядке выполняются блоки try/except/else/finally?"
        },
        {
            "answer": "очистка ресурсов",
            "hint": "finally для закрытия файлов, соединений и т.д.",
            "id": "m6t12",
            "question": "Для чего обычно используется finally?"
        }
    ],
    "theory": "**else и finally** — это дополнительные блоки в обработке исключений, которые делают код более надежным и читаемым. Они помогают структурировать логику и гарантировать выполнение важных операций!\n🔧 **Полная структура try/except/else/finally**:\n```python\ntry:\n    # Код, который может вызвать ошибку\n    risky_operation()\nexcept ТипОшибки:\n    # Обработка ошибки\n    handle_error()\nelse:\n    # Выполняется только если ошибки НЕ было\n    success_operation()\nfinally:\n    # Выполняется ВСЕГДА\n    cleanup_resources()\n```\n\n✅ **Блок else — выполняется при успехе**:\n\n**Простой пример**:\n```python\ndef divide_numbers(a, b):\n    try:\n        result = a / b\n    except ZeroDivisionError:\n        print(\"Ошибка: деление на ноль\")\n        return None\n    else:\n        print(f\"Деление выполнено успешно: {a} / {b} = {result}\")\n        return result\n    finally:\n        print(\"Операция деления завершена\")\n\n# Тестирование\nprint(divide_numbers(10, 2))  # Успех: else выполнится\nprint(divide_numbers(10, 0))  # Ошибка: else НЕ выполнится\n```\n\n**Обработка файлов с else**:\n```python\ndef process_file(filename):\n    file_handle = None\n    try:\n        file_handle = open(filename, 'r', encoding='utf-8')\n        content = file_handle.read()\n        \n        if not content.strip():\n            print(\"Файл пустой\")\n            return None\n            \n    except FileNotFoundError:\n        print(f\"Файл {filename} не найден\")\n        return None\n    except PermissionError:\n        print(f\"Нет прав доступа к файлу {filename}\")\n        return None\n    else:\n        # Выполняется только если файл успешно прочитан\n        print(f\"Файл {filename} успешно обработан\")\n        print(f\"Размер: {len(content)} символов\")\n        return content\n    finally:\n        # Всегда закрываем файл\n        if file_handle:\n            file_handle.close()\n            print(\"Файл закрыт\")\n\n# Тестирование\ncontent = process_file('data.txt')\nif content:\n    print(f\"Содержимое: {content[:50]}...\")\n```\n\n🔄 **Блок finally — выполняется всегда**:\n\n**Очистка ресурсов**:\n```python\ndef database_operation():\n    connection = None\n    try:\n        # Симуляция подключения к БД\n        connection = \"database_connection\"\n        print(\"Подключение к базе данных установлено\")\n        \n        # Симуляция операции\n        result = \"Данные успешно сохранены\"\n        print(result)\n        return result\n        \n    except Exception as e:\n        print(f\"Ошибка при работе с БД: {e}\")\n        return None\n    finally:\n        # Всегда закрываем соединение\n        if connection:\n            print(\"Соединение с базой данных закрыто\")\n            connection = None\n\n# Тестирование\nresult = database_operation()\n```\n\n**Логирование с finally**:\n```python\nimport time\nfrom datetime import datetime\n\ndef timed_operation(operation_name):\n    start_time = time.time()\n    try:\n        print(f\"Начинаем операцию: {operation_name}\")\n        \n        # Симуляция работы\n        time.sleep(0.1)  # Имитация обработки\n        \n        if operation_name == \"error_operation\":\n            raise ValueError(\"Искусственная ошибка\")\n            \n        print(f\"Операция {operation_name} завершена успешно\")\n        return True\n        \n    except Exception as e:\n        print(f\"Ошибка в операции {operation_name}: {e}\")\n        return False\n    finally:\n        # Всегда логируем время выполнения\n        end_time = time.time()\n        duration = end_time - start_time\n        timestamp = datetime.now().strftime('%Y-%m-%d %H:%M:%S')\n        print(f\"[{timestamp}] Операция '{operation_name}' заняла {duration:.3f} секунд\")\n\n# Тестирование\ntimed_operation(\"successful_operation\")\ntimed_operation(\"error_operation\")\n```\n\n💡 **Практические примеры**:\n\n**Безопасная работа с файлами**:\n```python\ndef backup_file(source_file, backup_dir='backups'):\n    import os\n    import shutil\n    from datetime import datetime\n    \n    source_handle = None\n    backup_handle = None\n    \n    try:\n        # Открываем исходный файл\n        source_handle = open(source_file, 'r', encoding='utf-8')\n        content = source_handle.read()\n        \n        # Создаем имя резервной копии\n        timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')\n        backup_filename = f\"{os.path.basename(source_file)}.backup_{timestamp}\"\n        backup_path = os.path.join(backup_dir, backup_filename)\n        \n        # Создаем директорию для бэкапов\n        os.makedirs(backup_dir, exist_ok=True)\n        \n        # Создаем резервную копию\n        backup_handle = open(backup_path, 'w', encoding='utf-8')\n        backup_handle.write(content)\n        backup_handle.flush()  # Принудительно записываем на диск\n        \n    except FileNotFoundError:\n        print(f\"Исходный файл {source_file} не найден\")\n        return False\n    except PermissionError:\n        print(f\"Нет прав доступа к файлу {source_file}\")\n        return False\n    except Exception as e:\n        print(f\"Ошибка при создании резервной копии: {e}\")\n        return False\n    else:\n        # Выполняется только при успешном создании бэкапа\n        print(f\"Резервная копия создана: {backup_path}\")\n        print(f\"Размер: {len(content)} символов\")\n        return True\n    finally:\n        # Всегда закрываем файлы\n        if source_handle:\n            source_handle.close()\n        if backup_handle:\n            backup_handle.close()\n        print(\"Файловые дескрипторы закрыты\")\n\n# Тестирование\nbackup_file('important_data.txt')\n```\n\n**Обработка сетевых соединений**:\n```python\ndef fetch_data_with_retry(url, max_retries=3):\n    import urllib.request\n    import time\n    \n    connection = None\n    retry_count = 0\n    \n    while retry_count < max_retries:\n        try:\n            print(f\"Попытка {retry_count + 1} из {max_retries}\")\n            \n            # Устанавливаем соединение\n            connection = urllib.request.urlopen(url, timeout=5)\n            data = connection.read().decode('utf-8')\n            \n        except urllib.error.URLError as e:\n            print(f\"Ошибка сети: {e}\")\n            retry_count += 1\n            if retry_count < max_retries:\n                print(f\"Повтор через 2 секунды...\")\n                time.sleep(2)\n            continue\n        except Exception as e:\n            print(f\"Неожиданная ошибка: {e}\")\n            return None\n        else:\n            # Успешное получение данных\n            print(f\"Данные успешно получены ({len(data)} байт)\")\n            return data\n        finally:\n            # Всегда закрываем соединение\n            if connection:\n                connection.close()\n                print(\"Соединение закрыто\")\n    \n    print(f\"Не удалось получить данные после {max_retries} попыток\")\n    return None\n\n# Тестирование\ndata = fetch_data_with_retry('https://httpbin.org/json')\nif data:\n    print(f\"Получены данные: {data[:100]}...\")\n```\n\n**Контекстный менеджер с finally**:\n```python\nclass DatabaseConnection:\n    def __init__(self, db_name):\n        self.db_name = db_name\n        self.connection = None\n    \n    def __enter__(self):\n        print(f\"Подключение к базе данных {self.db_name}\")\n        self.connection = f\"connection_to_{self.db_name}\"\n        return self\n    \n    def __exit__(self, exc_type, exc_val, exc_tb):\n        # Этот метод выполняется как finally\n        if self.connection:\n            print(f\"Закрытие соединения с {self.db_name}\")\n            self.connection = None\n        \n        if exc_type:\n            print(f\"Ошибка в контексте: {exc_type.__name__}: {exc_val}\")\n        else:\n            print(\"Операция завершена успешно\")\n        \n        return False  # Не подавляем исключения\n    \n    def execute_query(self, query):\n        if not self.connection:\n            raise RuntimeError(\"Нет активного соединения\")\n        print(f\"Выполняем запрос: {query}\")\n        return f\"Результат запроса: {query}\"\n\n# Использование\nwith DatabaseConnection('myapp') as db:\n    result = db.execute_query('SELECT * FROM users')\n    print(result)\n    # Соединение автоматически закроется в __exit__\n```\n\n**Обработка транзакций**:\n```python\ndef process_transaction(operations):\n    \"\"\"Обрабатывает транзакцию с откатом при ошибке\"\"\"\n    transaction_log = []\n    \n    try:\n        for i, operation in enumerate(operations):\n            print(f\"Выполняем операцию {i+1}: {operation}\")\n            \n            # Симуляция операции\n            if operation == \"error_operation\":\n                raise ValueError(\"Ошибка в операции\")\n            \n            # Логируем успешную операцию\n            transaction_log.append(f\"Операция {i+1} выполнена успешно\")\n            \n    except Exception as e:\n        print(f\"Ошибка в транзакции: {e}\")\n        print(\"Откатываем изменения...\")\n        \n        # Откатываем все выполненные операции\n        for log_entry in reversed(transaction_log):\n            print(f\"Откат: {log_entry}\")\n        \n        return False\n    else:\n        # Все операции выполнены успешно\n        print(\"Транзакция завершена успешно\")\n        print(f\"Выполнено операций: {len(transaction_log)}\")\n        return True\n    finally:\n        # Очищаем логи транзакции\n        transaction_log.clear()\n        print(\"Логи транзакции очищены\")\n\n# Тестирование\noperations = [\"operation1\", \"operation2\", \"operation3\"]\nprocess_transaction(operations)\n\noperations_with_error = [\"operation1\", \"error_operation\", \"operation3\"]\nprocess_transaction(operations_with_error)\n```\n\n⚠️ **Важные моменты**:\n- **else выполняется только при отсутствии исключений**\n- **finally выполняется всегда** — даже при return, break, continue\n- **finally не может предотвратить исключение** — только очистить ресурсы\n- **Порядок выполнения**: try → except/else → finally\n- **Используйте finally для очистки ресурсов**\n- **else полезен для кода, который должен выполняться только при успехе**\n- **Не используйте return в finally** — это может скрыть исключения",
    "title": "else/finally в обработке ошибок"
}
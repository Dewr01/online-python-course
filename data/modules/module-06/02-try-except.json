{
    "tasks": [
        {
            "answer": "try: x/0 except ZeroDivisionError: pass",
            "hint": "try: код except ТипОшибки: обработка",
            "id": "m6t5",
            "question": "Как обработать ошибку деления на ноль?"
        },
        {
            "answer": "except Exception:",
            "hint": "except Exception: ловит все исключения.",
            "id": "m6t6",
            "question": "Как поймать любую ошибку?"
        },
        {
            "answer": "except не выполнится",
            "hint": "except выполняется только при ошибке в try.",
            "id": "m6t7",
            "question": "Что произойдет если ошибка не произошла в try?"
        },
        {
            "answer": "да",
            "hint": "Можно ловить разные типы ошибок отдельно.",
            "id": "m6t8",
            "question": "Можно ли иметь несколько except блоков? (да/нет)"
        }
    ],
    "theory": "**try/except** — это основа обработки исключений в Python! Эта конструкция позволяет программе корректно реагировать на ошибки и продолжать работу.\n🔧 **Базовый синтаксис**:\n```python\ntry:\n    # Код, который может вызвать ошибку\n    risky_operation()\nexcept ТипОшибки:\n    # Обработка ошибки\n    handle_error()\n```\n\n**Простой пример**:\n```python\ntry:\n    number = int(input(\"Введите число: \"))\n    result = 10 / number\n    print(f\"Результат: {result}\")\nexcept ValueError:\n    print(\"Ошибка: введите корректное число\")\nexcept ZeroDivisionError:\n    print(\"Ошибка: деление на ноль\")\n```\n\n🎯 **Обработка конкретных типов ошибок**:\n\n**Множественные except блоки**:\n```python\ndef safe_divide(a, b):\n    try:\n        result = a / b\n        return result\n    except TypeError:\n        print(\"Ошибка: неверный тип данных\")\n        return None\n    except ZeroDivisionError:\n        print(\"Ошибка: деление на ноль\")\n        return None\n    except Exception as e:\n        print(f\"Неожиданная ошибка: {e}\")\n        return None\n\n# Тестирование\nprint(safe_divide(10, 2))    # 5.0\nprint(safe_divide(10, 0))    # None (ZeroDivisionError)\nprint(safe_divide(\"10\", 2))  # None (TypeError)\n```\n\n**Обработка нескольких типов в одном except**:\n```python\ndef process_data(data):\n    try:\n        # Попытка получить элемент по индексу\n        if isinstance(data, list):\n            return data[0]\n        elif isinstance(data, dict):\n            return data['value']\n        else:\n            return str(data)\n    except (IndexError, KeyError) as e:\n        print(f\"Ошибка доступа к данным: {e}\")\n        return None\n    except Exception as e:\n        print(f\"Общая ошибка: {e}\")\n        return None\n\n# Тестирование\nprint(process_data([1, 2, 3]))        # 1\nprint(process_data([]))               # None (IndexError)\nprint(process_data({'name': 'Анна'})) # None (KeyError)\nprint(process_data('строка'))         # строка\n```\n\n**Получение информации об ошибке**:\n```python\ndef analyze_file(filename):\n    try:\n        with open(filename, 'r', encoding='utf-8') as file:\n            content = file.read()\n            return len(content)\n    except FileNotFoundError as e:\n        print(f\"Файл не найден: {e}\")\n        return 0\n    except PermissionError as e:\n        print(f\"Нет прав доступа: {e}\")\n        return 0\n    except UnicodeDecodeError as e:\n        print(f\"Ошибка кодировки: {e}\")\n        return 0\n    except Exception as e:\n        print(f\"Неожиданная ошибка: {type(e).__name__}: {e}\")\n        return 0\n\n# Тестирование\nsize = analyze_file('nonexistent.txt')\nprint(f\"Размер файла: {size} символов\")\n```\n\n💡 **Практические примеры**:\n\n**Безопасный калькулятор**:\n```python\ndef safe_calculator(operation, a, b):\n    \"\"\"Безопасный калькулятор с обработкой ошибок\"\"\"\n    try:\n        # Преобразуем в числа\n        num_a = float(a)\n        num_b = float(b)\n        \n        # Выполняем операцию\n        if operation == '+':\n            return num_a + num_b\n        elif operation == '-':\n            return num_a - num_b\n        elif operation == '*':\n            return num_a * num_b\n        elif operation == '/':\n            if num_b == 0:\n                raise ValueError(\"Деление на ноль\")\n            return num_a / num_b\n        else:\n            raise ValueError(f\"Неизвестная операция: {operation}\")\n            \n    except ValueError as e:\n        print(f\"Ошибка ввода: {e}\")\n        return None\n    except Exception as e:\n        print(f\"Неожиданная ошибка: {e}\")\n        return None\n\n# Тестирование\nprint(safe_calculator('+', '5', '3'))     # 8.0\nprint(safe_calculator('/', '10', '0'))    # None (деление на ноль)\nprint(safe_calculator('*', 'abc', '2'))   # None (неверный ввод)\nprint(safe_calculator('^', '2', '3'))     # None (неизвестная операция)\n```\n\n**Обработка пользовательского ввода**:\n```python\ndef get_user_age():\n    \"\"\"Безопасно получает возраст пользователя\"\"\"\n    while True:\n        try:\n            age_input = input(\"Введите ваш возраст: \")\n            age = int(age_input)\n            \n            if age < 0:\n                print(\"Возраст не может быть отрицательным\")\n                continue\n            elif age > 150:\n                print(\"Возраст слишком большой\")\n                continue\n                \n            return age\n            \n        except ValueError:\n            print(\"Пожалуйста, введите число\")\n        except KeyboardInterrupt:\n            print(\"\\nОперация отменена\")\n            return None\n        except Exception as e:\n            print(f\"Произошла ошибка: {e}\")\n            return None\n\n# Использование\nage = get_user_age()\nif age is not None:\n    print(f\"Ваш возраст: {age} лет\")\n```\n\n**Обработка данных из файла**:\n```python\ndef process_student_grades(filename):\n    \"\"\"Обрабатывает оценки студентов из файла\"\"\"\n    students = []\n    \n    try:\n        with open(filename, 'r', encoding='utf-8') as file:\n            for line_num, line in enumerate(file, 1):\n                try:\n                    # Парсим строку: имя,оценка1,оценка2,оценка3\n                    parts = line.strip().split(',')\n                    if len(parts) != 4:\n                        print(f\"Строка {line_num}: неверный формат\")\n                        continue\n                    \n                    name = parts[0].strip()\n                    grades = [int(grade.strip()) for grade in parts[1:]]\n                    \n                    # Проверяем оценки\n                    if any(grade < 0 or grade > 100 for grade in grades):\n                        print(f\"Строка {line_num}: неверные оценки для {name}\")\n                        continue\n                    \n                    students.append({\n                        'name': name,\n                        'grades': grades,\n                        'average': sum(grades) / len(grades)\n                    })\n                    \n                except ValueError as e:\n                    print(f\"Строка {line_num}: ошибка преобразования - {e}\")\n                except Exception as e:\n                    print(f\"Строка {line_num}: неожиданная ошибка - {e}\")\n    \n    except FileNotFoundError:\n        print(f\"Файл {filename} не найден\")\n        return []\n    except Exception as e:\n        print(f\"Ошибка чтения файла: {e}\")\n        return []\n    \n    return students\n\n# Использование\nstudents = process_student_grades('grades.txt')\nfor student in students:\n    print(f\"{student['name']}: {student['average']:.1f}\")\n```\n\n**Обработка сетевых запросов**:\n```python\nimport urllib.request\nimport json\n\ndef fetch_user_data(user_id):\n    \"\"\"Получает данные пользователя с API\"\"\"\n    try:\n        url = f\"https://api.example.com/users/{user_id}\"\n        \n        with urllib.request.urlopen(url, timeout=10) as response:\n            data = json.loads(response.read().decode('utf-8'))\n            return data\n            \n    except urllib.error.HTTPError as e:\n        if e.code == 404:\n            print(f\"Пользователь {user_id} не найден\")\n        elif e.code == 403:\n            print(\"Нет прав доступа\")\n        else:\n            print(f\"HTTP ошибка {e.code}: {e.reason}\")\n        return None\n        \n    except urllib.error.URLError as e:\n        print(f\"Ошибка сети: {e.reason}\")\n        return None\n        \n    except json.JSONDecodeError as e:\n        print(f\"Ошибка парсинга JSON: {e}\")\n        return None\n        \n    except Exception as e:\n        print(f\"Неожиданная ошибка: {e}\")\n        return None\n\n# Использование\nuser_data = fetch_user_data(123)\nif user_data:\n    print(f\"Пользователь: {user_data.get('name', 'Неизвестно')}\")\n```\n\n⚠️ **Лучшие практики**:\n- **Обрабатывайте конкретные исключения** вместо общих\n- **Логируйте ошибки** для отладки\n- **Предоставляйте понятные сообщения** пользователю\n- **Не игнорируйте исключения** с `pass`\n- **Используйте `except Exception`** только в крайних случаях\n- **Проверяйте типы данных** перед операциями\n- **Тестируйте обработку ошибок**",
    "title": "try/except"
}
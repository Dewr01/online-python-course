{
  "title": "Собственные исключения",
  "theory": "**Собственные исключения** — это мощный инструмент для создания понятной и структурированной системы обработки ошибок в ваших приложениях. Они помогают сделать код более читаемым и упростить отладку!\n🔧 **Создание собственных исключений**:\n\n**Базовое исключение**:\n```python\nclass MyCustomError(Exception):\n    \"\"\"Базое пользовательское исключение\"\"\"\n    pass\n\n# Использование\nraise MyCustomError(\"Что-то пошло не так!\")\n```\n\n**Исключение с дополнительными атрибутами**:\n```python\nclass ValidationError(Exception):\n    \"\"\"Исключение для ошибок валидации\"\"\"\n    def __init__(self, message, field_name=None, value=None):\n        super().__init__(message)\n        self.field_name = field_name\n        self.value = value\n    \n    def __str__(self):\n        base_msg = super().__str__()\n        if self.field_name:\n            return f\"{base_msg} (поле: {self.field_name}, значение: {self.value})\"\n        return base_msg\n\n# Использование\nraise ValidationError(\"Неверный формат email\", \"email\", \"invalid-email\")\n```\n\n**Иерархия исключений**:\n```python\n# Базовое исключение для приложения\nclass AppError(Exception):\n    \"\"\"Базовое исключение приложения\"\"\"\n    pass\n\n# Исключения для разных модулей\nclass DatabaseError(AppError):\n    \"\"\"Ошибки базы данных\"\"\"\n    pass\n\nclass NetworkError(AppError):\n    \"\"\"Ошибки сети\"\"\"\n    pass\n\nclass ValidationError(AppError):\n    \"\"\"Ошибки валидации\"\"\"\n    pass\n\n# Специфичные исключения\nclass ConnectionError(DatabaseError):\n    \"\"\"Ошибка подключения к БД\"\"\"\n    pass\n\nclass QueryError(DatabaseError):\n    \"\"\"Ошибка выполнения запроса\"\"\"\n    pass\n\nclass TimeoutError(NetworkError):\n    \"\"\"Ошибка таймаута\"\"\"\n    pass\n```\n\n💡 **Практические примеры**:\n\n**Система валидации пользователей**:\n```python\nclass UserValidationError(Exception):\n    \"\"\"Базовое исключение валидации пользователя\"\"\"\n    pass\n\nclass InvalidEmailError(UserValidationError):\n    \"\"\"Неверный формат email\"\"\"\n    def __init__(self, email):\n        self.email = email\n        super().__init__(f\"Неверный формат email: {email}\")\n\nclass InvalidAgeError(UserValidationError):\n    \"\"\"Неверный возраст\"\"\"\n    def __init__(self, age):\n        self.age = age\n        super().__init__(f\"Возраст должен быть от 0 до 150 лет, получен: {age}\")\n\nclass PasswordTooWeakError(UserValidationError):\n    \"\"\"Слишком слабый пароль\"\"\"\n    def __init__(self, password):\n        self.password = password\n        super().__init__(\"Пароль должен содержать минимум 8 символов, включая цифры и буквы\")\n\ndef validate_user(email, age, password):\n    \"\"\"Валидирует данные пользователя\"\"\"\n    # Проверка email\n    if '@' not in email or '.' not in email.split('@')[1]:\n        raise InvalidEmailError(email)\n    \n    # Проверка возраста\n    if not isinstance(age, int) or age < 0 or age > 150:\n        raise InvalidAgeError(age)\n    \n    # Проверка пароля\n    if len(password) < 8 or not any(c.isdigit() for c in password):\n        raise PasswordTooWeakError(password)\n    \n    return True\n\n# Тестирование\nusers = [\n    (\"user@example.com\", 25, \"strongpass123\"),\n    (\"invalid-email\", 30, \"weak\"),\n    (\"test@test.com\", -5, \"goodpass456\"),\n    (\"admin@site.com\", 40, \"12345678\")\n]\n\nfor email, age, password in users:\n    try:\n        validate_user(email, age, password)\n        print(f\"✓ Пользователь {email} прошел валидацию\")\n    except UserValidationError as e:\n        print(f\"✗ Ошибка валидации: {e}\")\n```\n\n**Система обработки файлов**:\n```python\nclass FileProcessingError(Exception):\n    \"\"\"Базовое исключение обработки файлов\"\"\"\n    pass\n\nclass FileNotFoundError(FileProcessingError):\n    \"\"\"Файл не найден\"\"\"\n    def __init__(self, filename):\n        self.filename = filename\n        super().__init__(f\"Файл не найден: {filename}\")\n\nclass UnsupportedFileTypeError(FileProcessingError):\n    \"\"\"Неподдерживаемый тип файла\"\"\"\n    def __init__(self, filename, file_type):\n        self.filename = filename\n        self.file_type = file_type\n        super().__init__(f\"Неподдерживаемый тип файла {file_type} для файла {filename}\")\n\nclass FileCorruptedError(FileProcessingError):\n    \"\"\"Файл поврежден\"\"\"\n    def __init__(self, filename, reason):\n        self.filename = filename\n        self.reason = reason\n        super().__init__(f\"Файл {filename} поврежден: {reason}\")\n\ndef process_file(filename):\n    \"\"\"Обрабатывает файл с различными проверками\"\"\"\n    import os\n    \n    # Проверка существования файла\n    if not os.path.exists(filename):\n        raise FileNotFoundError(filename)\n    \n    # Проверка типа файла\n    allowed_extensions = ['.txt', '.csv', '.json']\n    file_ext = os.path.splitext(filename)[1].lower()\n    if file_ext not in allowed_extensions:\n        raise UnsupportedFileTypeError(filename, file_ext)\n    \n    # Проверка размера файла\n    file_size = os.path.getsize(filename)\n    if file_size == 0:\n        raise FileCorruptedError(filename, \"файл пустой\")\n    \n    if file_size > 10 * 1024 * 1024:  # 10MB\n        raise FileProcessingError(f\"Файл {filename} слишком большой: {file_size} байт\")\n    \n    # Симуляция обработки\n    print(f\"Файл {filename} успешно обработан\")\n    return True\n\n# Тестирование\nfiles_to_process = [\n    \"data.txt\",\n    \"nonexistent.csv\",\n    \"image.jpg\",\n    \"empty.json\",\n    \"huge_file.txt\"\n]\n\nfor filename in files_to_process:\n    try:\n        process_file(filename)\n    except FileProcessingError as e:\n        print(f\"Ошибка обработки файла: {e}\")\n```\n\n**Система банковских операций**:\n```python\nclass BankingError(Exception):\n    \"\"\"Базовое исключение банковской системы\"\"\"\n    pass\n\nclass InsufficientFundsError(BankingError):\n    \"\"\"Недостаточно средств\"\"\"\n    def __init__(self, account_id, balance, amount):\n        self.account_id = account_id\n        self.balance = balance\n        self.amount = amount\n        super().__init__(f\"Недостаточно средств на счете {account_id}. Баланс: {balance}, требуется: {amount}\")\n\nclass InvalidAccountError(BankingError):\n    \"\"\"Неверный номер счета\"\"\"\n    def __init__(self, account_id):\n        self.account_id = account_id\n        super().__init__(f\"Счет {account_id} не найден\")\n\nclass TransactionLimitError(BankingError):\n    \"\"\"Превышен лимит транзакции\"\"\"\n    def __init__(self, amount, limit):\n        self.amount = amount\n        self.limit = limit\n        super().__init__(f\"Сумма {amount} превышает лимит {limit}\")\n\nclass BankAccount:\n    def __init__(self, account_id, initial_balance=0, daily_limit=10000):\n        self.account_id = account_id\n        self.balance = initial_balance\n        self.daily_limit = daily_limit\n        self.daily_withdrawn = 0\n    \n    def withdraw(self, amount):\n        \"\"\"Снятие средств со счета\"\"\"\n        if amount <= 0:\n            raise BankingError(\"Сумма должна быть положительной\")\n        \n        if amount > self.daily_limit:\n            raise TransactionLimitError(amount, self.daily_limit)\n        \n        if self.daily_withdrawn + amount > self.daily_limit:\n            raise TransactionLimitError(amount, self.daily_limit - self.daily_withdrawn)\n        \n        if amount > self.balance:\n            raise InsufficientFundsError(self.account_id, self.balance, amount)\n        \n        self.balance -= amount\n        self.daily_withdrawn += amount\n        print(f\"Снято {amount} со счета {self.account_id}. Остаток: {self.balance}\")\n        return True\n    \n    def deposit(self, amount):\n        \"\"\"Пополнение счета\"\"\"\n        if amount <= 0:\n            raise BankingError(\"Сумма должна быть положительной\")\n        \n        self.balance += amount\n        print(f\"Пополнено {amount} на счет {self.account_id}. Баланс: {self.balance}\")\n        return True\n\n# Тестирование\naccount = BankAccount(\"12345\", 1000, 5000)\n\ntry:\n    account.withdraw(500)\n    account.withdraw(2000)\n    account.withdraw(3000)  # Превышение дневного лимита\nexcept BankingError as e:\n    print(f\"Банковская ошибка: {e}\")\n```\n\n**Система API с кастомными исключениями**:\n```python\nclass APIError(Exception):\n    \"\"\"Базовое исключение API\"\"\"\n    def __init__(self, message, status_code=None, response_data=None):\n        super().__init__(message)\n        self.status_code = status_code\n        self.response_data = response_data\n\nclass AuthenticationError(APIError):\n    \"\"\"Ошибка аутентификации\"\"\"\n    def __init__(self, message=\"Неверные учетные данные\"):\n        super().__init__(message, 401)\n\nclass RateLimitError(APIError):\n    \"\"\"Превышен лимит запросов\"\"\"\n    def __init__(self, retry_after=None):\n        message = f\"Превышен лимит запросов. Повторите через {retry_after} секунд\" if retry_after else \"Превышен лимит запросов\"\n        super().__init__(message, 429)\n        self.retry_after = retry_after\n\nclass ValidationError(APIError):\n    \"\"\"Ошибка валидации данных\"\"\"\n    def __init__(self, field_errors):\n        self.field_errors = field_errors\n        message = f\"Ошибки валидации: {field_errors}\"\n        super().__init__(message, 400)\n\ndef make_api_request(endpoint, data=None):\n    \"\"\"Выполняет API запрос с обработкой ошибок\"\"\"\n    # Симуляция различных ошибки API\n    if endpoint == \"/login\" and data.get(\"password\") != \"correct\":\n        raise AuthenticationError()\n    \n    if endpoint == \"/data\" and data and \"email\" not in data:\n        raise ValidationError({\"email\": \"Обязательное поле\"})\n    \n    if endpoint == \"/heavy\":\n        raise RateLimitError(60)\n    \n    print(f\"API запрос к {endpoint} выполнен успешно\")\n    return {\"status\": \"success\"}\n\n# Тестирование\nrequests = [\n    (\"/login\", {\"username\": \"user\", \"password\": \"wrong\"}),\n    (\"/data\", {\"name\": \"John\"}),\n    (\"/heavy\", {}),\n    (\"/users\", {\"email\": \"user@example.com\"})\n]\n\nfor endpoint, data in requests:\n    try:\n        result = make_api_request(endpoint, data)\n        print(f\"✓ {endpoint}: {result}\")\n    except APIError as e:\n        print(f\"✗ {endpoint}: {e} (код: {e.status_code})\")\n```\n\n⚠️ **Лучшие практики создания исключений**:\n- **Наследуйтесь от подходящего базового класса** (Exception, ValueError, RuntimeError)\n- **Создавайте иерархию исключений** для логической группировки\n- **Добавляйте полезные атрибуты** для отладки\n- **Переопределяйте `__str__`** для понятных сообщений\n- **Используйте описательные имена** классов исключений\n- **Документируйте исключения** в docstring\n- **Не создавайте слишком много исключений** — используйте разумно\n- **Тестируйте обработку исключений** в вашем коде",
  "tasks": [
    {
      "id": "m6t13",
      "question": "Как создать собственное исключение MyError?",
      "answer": "class MyError(Exception): pass",
      "hint": "Наследование от Exception."
    },
    {
      "id": "m6t14",
      "question": "Как выбросить собственное исключение?",
      "answer": "raise MyError('message')",
      "hint": "raise Исключение('сообщение')."
    },
    {
      "id": "m6t15",
      "question": "От какого класса наследуются исключения?",
      "answer": "Exception",
      "hint": "Все исключения наследуются от Exception."
    },
    {
      "id": "m6t16",
      "question": "Можно ли передать сообщение в собственное исключение? (да/нет)",
      "answer": "да",
      "hint": "raise MyError('текст сообщения')."
    }
  ]
}
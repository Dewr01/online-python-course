{
    "tasks": [
        {
            "answer": "NameError",
            "hint": "NameError — переменная не определена.",
            "id": "m6t1",
            "question": "Какая ошибка при print(undefined_variable)?"
        },
        {
            "answer": "IndexError",
            "hint": "IndexError — выход за границы списка.",
            "id": "m6t2",
            "question": "Какая ошибка при [1,2,3][10]?"
        },
        {
            "answer": "ValueError",
            "hint": "ValueError — нельзя преобразовать 'abc' в число.",
            "id": "m6t3",
            "question": "Какая ошибка при int('abc')?"
        },
        {
            "answer": "KeyError",
            "hint": "KeyError — ключ 'missing' не найден в словаре.",
            "id": "m6t4",
            "question": "Какая ошибка при {}['missing']?"
        }
    ],
    "theory": "**Ошибки и исключения** — это неотъемлемая часть программирования! Python предоставляет мощную систему обработки ошибок, которая помогает создавать надежные приложения.\n🚨 **Основные типы ошибки в Python**:\n\n**SyntaxError — синтаксические ошибки**:\n```python\n# Неправильный синтаксис\nif True\n    print(\"Ошибка!\")\n# SyntaxError: expected ':'\n\n# Незакрытые скобки\nprint(\"Привет мир\"\n# SyntaxError: unexpected EOF while parsing\n```\n\n**NameError — неопределенные переменные**:\n```python\n# Переменная не определена\nprint(undefined_variable)\n# NameError: name 'undefined_variable' is not defined\n\n# Опечатка в имени\nname = \"Анна\"\nprint(nam)  # NameError: name 'nam' is not defined\n```\n\n**TypeError — неправильные типы данных**:\n```python\n# Несовместимые типы\nresult = \"5\" + 3\n# TypeError: can only concatenate str (not \"int\") to str\n\n# Неправильное количество аргументов\nlen()  # TypeError: len() takes exactly one argument (0 given)\n\n# Вызов несуществующего метода\nnumber = 42\nnumber.append(1)  # TypeError: 'int' object has no attribute 'append'\n```\n\n**ValueError — неправильные значения**:\n```python\n# Невозможно преобразовать строку в число\nint(\"abc\")\n# ValueError: invalid literal for int() with base 10: 'abc'\n\n# Неправильный индекс\nint(\"3.14\")\n# ValueError: invalid literal for int() with base 10: '3.14'\n\n# Пустая строка в int()\nint(\"\")\n# ValueError: invalid literal for int() with base 10: ''\n```\n\n**IndexError — выход за границы последовательности**:\n```python\n# Выход за границы списка\nnumbers = [1, 2, 3]\nprint(numbers[10])\n# IndexError: list index out of range\n\n# Пустой список\nempty_list = []\nprint(empty_list[0])\n# IndexError: list index out of range\n\n# Отрицательный индекс больше длины\nprint(numbers[-10])\n# IndexError: list index out of range\n```\n\n**KeyError — отсутствующий ключ в словаре**:\n```python\n# Ключ не существует\nperson = {\"name\": \"Анна\", \"age\": 25}\nprint(person[\"salary\"])\n# KeyError: 'salary'\n\n# Пустой словарь\nempty_dict = {}\nprint(empty_dict[\"key\"])\n# KeyError: 'key'\n```\n\n**ZeroDivisionError — деление на ноль**:\n```python\n# Деление на ноль\nresult = 10 / 0\n# ZeroDivisionError: division by zero\n\n# Целочисленное деление на ноль\nresult = 10 // 0\n# ZeroDivisionError: integer division or modulo by zero\n```\n\n**FileNotFoundError — файл не найден**:\n```python\n# Файл не существует\nwith open('nonexistent.txt', 'r') as file:\n    content = file.read()\n# FileNotFoundError: [Errno 2] No such file or directory: 'nonexistent.txt'\n```\n\n**PermissionError — нет прав доступа**:\n```python\n# Нет прав на запись\nwith open('/root/readonly.txt', 'w') as file:\n    file.write('test')\n# PermissionError: [Errno 13] Permission denied: '/root/readonly.txt'\n```\n\n💡 **Практические примеры диагностики ошибок**:\n\n**Анализ ошибок в коде**:\n```python\ndef analyze_code_errors():\n    \"\"\"Демонстрирует различные типы ошибок\"\"\"\n    \n    # 1. NameError\n    try:\n        print(undefined_variable)\n    except NameError as e:\n        print(f\"NameError: {e}\")\n    \n    # 2. TypeError\n    try:\n        result = \"5\" + 3\n    except TypeError as e:\n        print(f\"TypeError: {e}\")\n    \n    # 3. ValueError\n    try:\n        number = int(\"abc\")\n    except ValueError as e:\n        print(f\"ValueError: {e}\")\n    \n    # 4. IndexError\n    try:\n        numbers = [1, 2, 3]\n        print(numbers[10])\n    except IndexError as e:\n        print(f\"IndexError: {e}\")\n    \n    # 5. KeyError\n    try:\n        person = {\"name\": \"Анна\"}\n        print(person[\"age\"])\n    except KeyError as e:\n        print(f\"KeyError: {e}\")\n    \n    # 6. ZeroDivisionError\n    try:\n        result = 10 / 0\n    except ZeroDivisionError as e:\n        print(f\"ZeroDivisionError: {e}\")\n\nanalyze_code_errors()\n```\n\n**Проверка типов данных**:\n```python\ndef safe_operations(data):\n    \"\"\"Безопасно выполняет операции с данными\"\"\"\n    \n    # Проверка типа перед операцией\n    if isinstance(data, str):\n        print(f\"Строка: {data.upper()}\")\n    elif isinstance(data, list):\n        print(f\"Список длиной {len(data)}: {data}\")\n    elif isinstance(data, dict):\n        print(f\"Словарь с ключами: {list(data.keys())}\")\n    else:\n        print(f\"Неизвестный тип: {type(data)}\")\n\n# Тестирование\nsafe_operations(\"Привет\")\nsafe_operations([1, 2, 3])\nsafe_operations({\"name\": \"Анна\"})\nsafe_operations(42)\n```\n\n**Валидация пользовательского ввода**:\n```python\ndef validate_user_input(value, expected_type):\n    \"\"\"Валидирует пользовательский ввод\"\"\"\n    try:\n        if expected_type == 'int':\n            return int(value)\n        elif expected_type == 'float':\n            return float(value)\n        elif expected_type == 'list':\n            return eval(value)  # Осторожно! В реальном коде используйте ast.literal_eval\n        else:\n            return value\n    except ValueError as e:\n        print(f\"Ошибка преобразования: {e}\")\n        return None\n    except Exception as e:\n        print(f\"Неожиданная ошибка: {e}\")\n        return None\n\n# Тестирование\nage = validate_user_input(\"25\", 'int')\nif age is not None:\n    print(f\"Возраст: {age}\")\n\nprice = validate_user_input(\"abc\", 'float')\nif price is None:\n    print(\"Неверный формат цены\")\n```\n\n🔍 **Иерархия исключений**:\n```python\n# Все исключения наследуются от BaseException\n# ├── Exception (все обычные исключения)\n# │   ├── ArithmeticError\n# │   │   └── ZeroDivisionError\n# │   ├── LookupError\n# │   │   ├── IndexError\n# │   │   └── KeyError\n# │   ├── OSError\n# │   │   ├── FileNotFoundError\n# │   │   └── PermissionError\n# │   ├── ValueError\n# │   ├── TypeError\n# │   └── NameError\n# └── SystemExit, KeyboardInterrupt, GeneratorExit\n```\n\n⚠️ **Важно помнить**:\n- **SyntaxError** возникает до выполнения кода\n- **NameError** — переменная не определена или опечатка\n- **TypeError** — неправильный тип данных для операции\n- **ValueError** — правильный тип, но неправильное значение\n- **IndexError/KeyError** — проблемы с доступом к элементам\n- **Всегда обрабатывайте исключения** в продакшн коде\n- **Используйте конкретные типы** исключений вместо общих",
    "title": "Типы ошибок"
}
{
    "tasks": [
        {
            "answer": "open('file.txt', 'r')",
            "hint": "open(имя_файла, режим)",
            "id": "m5t9",
            "question": "Как открыть файл для чтения?"
        },
        {
            "answer": "'w'",
            "hint": "'w' — write (запись).",
            "id": "m5t10",
            "question": "Какой режим для записи в файл?"
        },
        {
            "answer": "read()",
            "hint": "Метод read() читает весь файл.",
            "id": "m5t11",
            "question": "Как прочитать весь файл?"
        },
        {
            "answer": "write()",
            "hint": "Метод write(строка) записывает в файл.",
            "id": "m5t12",
            "question": "Как записать строку в файл?"
        }
    ],
    "theory": "**Работа с файлами** — это основа для сохранения и загрузки данных в Python. Файлы позволяют хранить информацию между запусками программы и обмениваться данными с другими приложениями!\n📁 **Открытие файлов с помощью open()**:\n\n**Основной синтаксис**:\n```python\nfile = open('filename.txt', 'mode')\n# Работа с файлом\nfile.close()  # Важно закрыть файл!\n```\n\n**Режимы открытия файлов**:\n```python\n# 'r' — чтение (по умолчанию)\nfile = open('data.txt', 'r')\n\n# 'w' — запись (перезаписывает файл)\nfile = open('output.txt', 'w')\n\n# 'a' — добавление в конец файла\nfile = open('log.txt', 'a')\n\n# 'x' — создание нового файла (ошибка если существует)\nfile = open('new_file.txt', 'x')\n\n# 'b' — бинарный режим (для изображений, видео)\nfile = open('image.jpg', 'rb')\n\n# Комбинированные режимы\nfile = open('data.txt', 'r+')  # чтение и запись\nfile = open('log.txt', 'a+')   # добавление и чтение\n```\n\n📖 **Чтение из файлов**:\n\n**read() — чтение всего файла**:\n```python\n# Чтение всего файла\nwith open('poem.txt', 'r', encoding='utf-8') as file:\n    content = file.read()\n    print(content)\n```\n\n**readline() — чтение по одной строке**:\n```python\n# Чтение построчно\nwith open('students.txt', 'r', encoding='utf-8') as file:\n    line1 = file.readline()  # Первая строка\n    line2 = file.readline()  # Вторая строка\n    print(f\"Первая строка: {line1.strip()}\")\n    print(f\"Вторая строка: {line2.strip()}\")\n```\n\n**readlines() — чтение всех строк в список**:\n```python\n# Чтение всех строк\nwith open('config.txt', 'r', encoding='utf-8') as file:\n    lines = file.readlines()\n    for i, line in enumerate(lines, 1):\n        print(f\"Строка {i}: {line.strip()}\")\n```\n\n**Итерация по файлу**:\n```python\n# Самый эффективный способ для больших файлов\nwith open('big_file.txt', 'r', encoding='utf-8') as file:\n    for line_number, line in enumerate(file, 1):\n        if 'error' in line.lower():\n            print(f\"Ошибка в строке {line_number}: {line.strip()}\")\n```\n\n✏️ **Запись в файлы**:\n\n**write() — запись строки**:\n```python\n# Запись в файл\nwith open('output.txt', 'w', encoding='utf-8') as file:\n    file.write('Привет, мир!\\n')\n    file.write('Это вторая строка\\n')\n    file.write('И третья строка')\n```\n\n**writelines() — запись списка строк**:\n```python\n# Запись списка строк\nlines = [\n    'Первая строка\\n',\n    'Вторая строка\\n',\n    'Третья строка\\n'\n]\n\nwith open('multiple_lines.txt', 'w', encoding='utf-8') as file:\n    file.writelines(lines)\n```\n\n**Добавление в файл**:\n```python\n# Добавление в конец файла\nwith open('log.txt', 'a', encoding='utf-8') as file:\n    file.write(f\"{datetime.now()}: Пользователь вошел в систему\\n\")\n```\n\n💡 **Практические примеры**:\n\n**Копирование файла**:\n```python\ndef copy_file(source, destination):\n    \"\"\"Копирует содержимое одного файла в другой\"\"\"\n    with open(source, 'r', encoding='utf-8') as src:\n        with open(destination, 'w', encoding='utf-8') as dst:\n            dst.write(src.read())\n    print(f\"Файл {source} скопирован в {destination}\")\n\ncopy_file('original.txt', 'copy.txt')\n```\n\n**Поиск в файле**:\n```python\ndef search_in_file(filename, search_term):\n    \"\"\"Ищет строки, содержащие определенный текст\"\"\"\n    matching_lines = []\n    \n    with open(filename, 'r', encoding='utf-8') as file:\n        for line_number, line in enumerate(file, 1):\n            if search_term.lower() in line.lower():\n                matching_lines.append((line_number, line.strip()))\n    \n    return matching_lines\n\n# Поиск слова \"Python\" в файле\nresults = search_in_file('code.py', 'Python')\nfor line_num, line in results:\n    print(f\"Строка {line_num}: {line}\")\n```\n\n**Создание резервной копии**:\n```python\nimport shutil\nfrom datetime import datetime\n\ndef backup_file(filename):\n    \"\"\"Создает резервную копию файла с timestamp\"\"\"\n    timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')\n    backup_name = f\"{filename}.backup_{timestamp}\"\n    \n    shutil.copy2(filename, backup_name)\n    print(f\"Создана резервная копия: {backup_name}\")\n    return backup_name\n\nbackup_file('important_data.txt')\n```\n\n**Обработка CSV файлов**:\n```python\ndef read_csv(filename):\n    \"\"\"Читает CSV файл и возвращает список словарей\"\"\"\n    data = []\n    \n    with open(filename, 'r', encoding='utf-8') as file:\n        # Читаем заголовки\n        headers = file.readline().strip().split(',')\n        \n        # Читаем данные\n        for line in file:\n            values = line.strip().split(',')\n            row = dict(zip(headers, values))\n            data.append(row)\n    \n    return data\n\ndef write_csv(filename, data, headers):\n    \"\"\"Записывает данные в CSV файл\"\"\"\n    with open(filename, 'w', encoding='utf-8') as file:\n        # Записываем заголовки\n        file.write(','.join(headers) + '\\n')\n        \n        # Записываем данные\n        for row in data:\n            values = [str(row[header]) for header in headers]\n            file.write(','.join(values) + '\\n')\n\n# Пример использования\nstudents = [\n    {'name': 'Анна', 'age': 20, 'grade': 85},\n    {'name': 'Борис', 'age': 22, 'grade': 92}\n]\n\nwrite_csv('students.csv', students, ['name', 'age', 'grade'])\nloaded_data = read_csv('students.csv')\nprint(loaded_data)\n```\n\n**Логирование в файл**:\n```python\ndef log_to_file(message, level='INFO'):\n    \"\"\"Записывает сообщение в лог-файл\"\"\"\n    timestamp = datetime.now().strftime('%Y-%m-%d %H:%M:%S')\n    log_entry = f\"[{timestamp}] {level}: {message}\\n\"\n    \n    with open('app.log', 'a', encoding='utf-8') as file:\n        file.write(log_entry)\n\n# Использование\nlog_to_file('Приложение запущено')\nlog_to_file('Ошибка подключения к БД', 'ERROR')\nlog_to_file('Пользователь авторизован')\n```\n\n⚠️ **Важные моменты**:\n- **Всегда закрывайте файлы** или используйте `with`\n- **Указывайте кодировку** (`encoding='utf-8'`) для текстовых файлов\n- **Обрабатывайте исключения** при работе с файлами\n- **Проверяйте существование файла** перед чтением\n- **Используйте `with`** — это самый безопасный способ\n\n🔧 **Обработка ошибок**:\n```python\ntry:\n    with open('nonexistent.txt', 'r') as file:\n        content = file.read()\nexcept FileNotFoundError:\n    print(\"Файл не найден!\")\nexcept PermissionError:\n    print(\"Нет прав доступа к файлу!\")\nexcept Exception as e:\n    print(f\"Произошла ошибка: {e}\")\n```",
    "title": "Работа с файлами"
}
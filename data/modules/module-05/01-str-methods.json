{
    "tasks": [
        {
            "answer": "['hello', 'world']",
            "hint": "split() разделяет строку по пробелам.",
            "id": "m5t1",
            "question": "Что получится при 'hello world'.split()?"
        },
        {
            "answer": "a b",
            "hint": "join() объединяет элементы списка через разделитель.",
            "id": "m5t2",
            "question": "Что получится при ' '.join(['a', 'b'])?"
        },
        {
            "answer": "hello",
            "hint": "strip() удаляет пробелы с начала и конца строки.",
            "id": "m5t3",
            "question": "Что получится при '  hello  '.strip()?"
        },
        {
            "answer": "hello",
            "hint": "lower() преобразует в нижний регистр.",
            "id": "m5t4",
            "question": "Что получится при 'Hello'.lower()?"
        }
    ],
    "theory": "**Методы строк** — это встроенные функции для работы с текстом в Python. Строки в Python неизменяемые (immutable), поэтому методы возвращают новые строки!\n🔧 **Основные методы строк**:\n\n**Разделение и объединение**:\n```python\ntext = \"Привет, мир! Как дела?\"\n\n# Разделение по пробелам\nwords = text.split()\nprint(words)  # ['Привет,', 'мир!', 'Как', 'дела?']\n\n# Разделение по запятой\nparts = text.split(',')\nprint(parts)  # ['Привет', ' мир! Как дела?']\n\n# Объединение обратно\njoined = ' '.join(words)\nprint(joined)  # Привет, мир! Как дела?\n\n# Объединение с другим разделителем\njoined_dash = '-'.join(words)\nprint(joined_dash)  # Привет,-мир!-Как-дела?\n```\n\n**Очистка от пробелов**:\n```python\nmessy_text = \"  \\n  Привет, мир!  \\t  \"\n\n# Удаление пробелов с начала и конца\nclean = messy_text.strip()\nprint(f\"'{clean}'\")  # 'Привет, мир!'\n\n# Удаление только слева\nleft_clean = messy_text.lstrip()\nprint(f\"'{left_clean}'\")  # 'Привет, мир!  \\t  '\n\n# Удаление только справа\nright_clean = messy_text.rstrip()\nprint(f\"'{right_clean}'\")  # '  \\n  Привет, мир!'\n```\n\n**Изменение регистра**:\n```python\nname = \"Анна Петрова\"\n\nprint(name.upper())    # АННА ПЕТРОВА\nprint(name.lower())    # анна петрова\nprint(name.title())    # Анна Петрова\nprint(name.capitalize()) # Анна петрова\nprint(name.swapcase())  # аННА пЕТРОВА\n```\n\n**Поиск и замена**:\n```python\ntext = \"Python — отличный язык программирования\"\n\n# Поиск подстроки\npos = text.find(\"Python\")\nprint(pos)  # 0 (позиция начала)\n\npos = text.find(\"Java\")\nprint(pos)  # -1 (не найдено)\n\n# Проверка начала и конца\nprint(text.startswith(\"Python\"))  # True\nprint(text.endswith(\"программирования\"))  # True\n\n# Замена текста\nnew_text = text.replace(\"Python\", \"JavaScript\")\nprint(new_text)  # JavaScript — отличный язык программирования\n\n# Замена только первого вхождения\nfirst_replace = text.replace(\"а\", \"о\", 1)\nprint(first_replace)  # Python — отличный язык прогроммирования\n```\n\n**Проверка содержимого**:\n```python\nemail = \"user@example.com\"\nnumber = \"12345\"\nspaces = \"   \"\n\n# Проверка типа символов\nprint(email.isalnum())    # False (содержит @ и .)\nprint(number.isdigit())   # True\nprint(number.isnumeric()) # True\nprint(spaces.isspace())   # True\nprint(email.islower())    # True\nprint(\"HELLO\".isupper())  # True\n```\n\n💡 **Практические примеры**:\n\n**Обработка пользовательского ввода**:\n```python\ndef clean_user_input(text):\n    \"\"\"Очищает и нормализует пользовательский ввод\"\"\"\n    # Удаляем лишние пробелы\n    cleaned = text.strip()\n    \n    # Приводим к нижнему регистру\n    cleaned = cleaned.lower()\n    \n    # Заменяем множественные пробелы на одинарные\n    cleaned = ' '.join(cleaned.split())\n    \n    return cleaned\n\nuser_input = \"  ПРИВЕТ    МИР!  \"\nclean_input = clean_user_input(user_input)\nprint(f\"'{clean_input}'\")  # 'привет мир!'\n```\n\n**Парсинг CSV данных**:\n```python\ndef parse_csv_line(line):\n    \"\"\"Парсит строку CSV\"\"\"\n    # Разделяем по запятым\n    fields = line.split(',')\n    \n    # Очищаем каждое поле от пробелов\n    clean_fields = [field.strip() for field in fields]\n    \n    return clean_fields\n\ncsv_line = \"Иван, 25, Москва, Программист\"\nparsed = parse_csv_line(csv_line)\nprint(parsed)  # ['Иван', '25', 'Москва', 'Программист']\n```\n\n**Валидация email**:\n```python\ndef is_valid_email(email):\n    \"\"\"Проверяет базовую валидность email\"\"\"\n    email = email.strip().lower()\n    \n    # Должен содержать @\n    if '@' not in email:\n        return False\n    \n    # Должен содержать точку после @\n    if '.' not in email.split('@')[1]:\n        return False\n    \n    # Не должен содержать пробелы\n    if ' ' in email:\n        return False\n    \n    return True\n\nprint(is_valid_email(\"user@example.com\"))  # True\nprint(is_valid_email(\"invalid-email\"))     # False\nprint(is_valid_email(\"user @example.com\")) # False\n```\n\n**Подсчет слов**:\n```python\ndef count_words(text):\n    \"\"\"Подсчитывает количество слов в тексте\"\"\"\n    # Разделяем по пробелам и убираем пустые строки\n    words = [word for word in text.split() if word]\n    return len(words)\n\ndef count_sentences(text):\n    \"\"\"Подсчитывает количество предложений\"\"\"\n    # Считаем точки, восклицательные и вопросительные знаки\n    sentences = text.count('.') + text.count('!') + text.count('?')\n    return sentences\n\narticle = \"Python — отличный язык! Он простой и мощный. Попробуйте его!\"\nprint(f\"Слов: {count_words(article)}\")      # Слов: 9\nprint(f\"Предложений: {count_sentences(article)}\")  # Предложений: 3\n```\n\n**Форматирование телефонного номера**:\n```python\ndef format_phone(phone):\n    \"\"\"Форматирует номер телефона\"\"\"\n    # Убираем все нецифровые символы\n    digits = ''.join([c for c in phone if c.isdigit()])\n    \n    if len(digits) == 11 and digits.startswith('7'):\n        # Российский номер: +7 (XXX) XXX-XX-XX\n        return f\"+7 ({digits[1:4]}) {digits[4:7]}-{digits[7:9]}-{digits[9:11]}\"\n    elif len(digits) == 10:\n        # Номер без кода страны: (XXX) XXX-XX-XX\n        return f\"({digits[0:3]}) {digits[3:6]}-{digits[6:8]}-{digits[8:10]}\"\n    else:\n        return \"Неверный формат номера\"\n\nprint(format_phone(\"8-999-123-45-67\"))    # +7 (999) 123-45-67\nprint(format_phone(\"9991234567\"))          # (999) 123-45-67\nprint(format_phone(\"123\"))                 # Неверный формат номера\n```\n\n⚠️ **Важно помнить**:\n- Строки неизменяемые — методы возвращают новые строки\n- Методы чувствительны к регистру\n- `find()` возвращает -1 если не найдено\n- `split()` без аргументов разделяет по любым пробельным символам\n- `strip()` удаляет пробелы, табуляции и переносы строк",
    "title": "Методы строк"
}
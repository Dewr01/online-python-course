{
    "tasks": [
        {
            "answer": "with open('file.txt') as f:",
            "hint": "with open(имя_файла) as переменная:",
            "id": "m5t13",
            "question": "Как правильно открыть файл с автоматическим закрытием?"
        },
        {
            "answer": "Автоматически закрывает файл",
            "hint": "Управляет ресурсами.",
            "id": "m5t14",
            "question": "Что делает конструкция with?"
        },
        {
            "answer": "with open('a.txt') as a, open('b.txt') as b:",
            "hint": "Несколько менеджеров через запятую.",
            "id": "m5t15",
            "question": "Как открыть два файла одновременно с with?"
        },
        {
            "answer": "Файл все равно закроется",
            "hint": "with гарантирует закрытие.",
            "id": "m5t16",
            "question": "Что произойдет с файлом при ошибке внутри with?"
        }
    ],
    "theory": "**Контекстный менеджер `with`** — это элегантный и безопасный способ работы с ресурсами в Python. Он автоматически управляет открытием и закрытием файлов, подключений к базам данных и других ресурсов!\n🔧 **Основной синтаксис**:\n```python\nwith open('file.txt', 'r') as file:\n    # Работа с файлом\n    content = file.read()\n    print(content)\n# Файл автоматически закрывается здесь!\n```\n\n**Преимущества `with`**:\n- ✅ **Автоматическое закрытие** — файл закрывается даже при ошибках\n- ✅ **Безопасность** — исключения не нарушают работу с ресурсами\n- ✅ **Читаемость** — код становится более понятным\n- ✅ **Надежность** — гарантированное освобождение ресурсов\n\n📁 **Работа с файлами**:\n\n**Чтение файла**:\n```python\n# Безопасное чтение\nwith open('poem.txt', 'r', encoding='utf-8') as file:\n    content = file.read()\n    print(f\"Размер файла: {len(content)} символов\")\n    print(f\"Количество строк: {content.count(chr(10)) + 1}\")\n# Файл автоматически закрыт\n```\n\n**Запись в файл**:\n```python\n# Безопасная запись\ndata = [\"Строка 1\", \"Строка 2\", \"Строка 3\"]\n\nwith open('output.txt', 'w', encoding='utf-8') as file:\n    for line in data:\n        file.write(line + '\\n')\n    print(\"Данные записаны в файл\")\n# Файл автоматически закрыт и сохранен\n```\n\n**Обработка больших файлов**:\n```python\n# Эффективная обработка больших файлов\nwith open('big_file.txt', 'r', encoding='utf-8') as file:\n    line_count = 0\n    word_count = 0\n    \n    for line in file:\n        line_count += 1\n        word_count += len(line.split())\n        \n        # Обрабатываем каждую строку\n        if 'error' in line.lower():\n            print(f\"Ошибка в строке {line_count}: {line.strip()}\")\n    \n    print(f\"Обработано {line_count} строк, {word_count} слов\")\n```\n\n🔄 **Работа с несколькими файлами**:\n\n**Копирование файла**:\n```python\n# Копирование с автоматическим управлением ресурсами\nwith open('source.txt', 'r', encoding='utf-8') as source:\n    with open('copy.txt', 'w', encoding='utf-8') as destination:\n        content = source.read()\n        destination.write(content)\n        print(\"Файл успешно скопирован\")\n# Оба файла автоматически закрыты\n```\n\n**Сравнение файлов**:\n```python\ndef compare_files(file1_path, file2_path):\n    \"\"\"Сравнивает содержимое двух файлов\"\"\"\n    with open(file1_path, 'r', encoding='utf-8') as file1:\n        with open(file2_path, 'r', encoding='utf-8') as file2:\n            content1 = file1.read()\n            content2 = file2.read()\n            \n            if content1 == content2:\n                print(\"Файлы идентичны\")\n                return True\n            else:\n                print(\"Файлы различаются\")\n                return False\n\ncompare_files('file1.txt', 'file2.txt')\n```\n\n**Объединение файлов**:\n```python\ndef merge_files(output_path, *input_paths):\n    \"\"\"Объединяет несколько файлов в один\"\"\"\n    with open(output_path, 'w', encoding='utf-8') as output_file:\n        for input_path in input_paths:\n            try:\n                with open(input_path, 'r', encoding='utf-8') as input_file:\n                    output_file.write(f\"--- {input_path} ---\\n\")\n                    output_file.write(input_file.read())\n                    output_file.write('\\n\\n')\n                    print(f\"Добавлен файл: {input_path}\")\n            except FileNotFoundError:\n                print(f\"Файл {input_path} не найден, пропускаем\")\n\nmerge_files('merged.txt', 'file1.txt', 'file2.txt', 'file3.txt')\n```\n\n💡 **Практические примеры**:\n\n**Логирование с ротацией**:\n```python\nimport datetime\nimport os\n\ndef log_message(message, log_dir='logs'):\n    \"\"\"Записывает сообщение в лог-файл с датой в имени\"\"\"\n    # Создаем директорию если не существует\n    os.makedirs(log_dir, exist_ok=True)\n    \n    # Формируем имя файла с текущей датой\n    date_str = datetime.datetime.now().strftime('%Y-%m-%d')\n    log_file = os.path.join(log_dir, f'app_{date_str}.log')\n    \n    # Формируем запись лога\n    timestamp = datetime.datetime.now().strftime('%Y-%m-%d %H:%M:%S')\n    log_entry = f\"[{timestamp}] {message}\\n\"\n    \n    # Записываем в файл\n    with open(log_file, 'a', encoding='utf-8') as file:\n        file.write(log_entry)\n    \n    print(f\"Сообщение записано в {log_file}\")\n\n# Использование\nlog_message(\"Приложение запущено\")\nlog_message(\"Пользователь выполнил действие\")\nlog_message(\"Ошибка при обработке данных\")\n```\n\n**Обработка конфигурационных файлов**:\n```python\nimport json\n\ndef load_config(config_path='config.json'):\n    \"\"\"Загружает конфигурацию из JSON файла\"\"\"\n    try:\n        with open(config_path, 'r', encoding='utf-8') as file:\n            config = json.load(file)\n            print(f\"Конфигурация загружена из {config_path}\")\n            return config\n    except FileNotFoundError:\n        print(f\"Конфигурационный файл {config_path} не найден\")\n        return {}\n    except json.JSONDecodeError:\n        print(f\"Ошибка парсинга JSON в файле {config_path}\")\n        return {}\n\ndef save_config(config, config_path='config.json'):\n    \"\"\"Сохраняет конфигурацию в JSON файл\"\"\"\n    with open(config_path, 'w', encoding='utf-8') as file:\n        json.dump(config, file, indent=4, ensure_ascii=False)\n        print(f\"Конфигурация сохранена в {config_path}\")\n\n# Пример использования\nconfig = load_config()\nif not config:\n    config = {\n        'database': {\n            'host': 'localhost',\n            'port': 5432,\n            'name': 'myapp'\n        },\n        'settings': {\n            'debug': True,\n            'max_users': 100\n        }\n    }\n    save_config(config)\n```\n\n**Обработка CSV данных**:\n```python\nimport csv\n\ndef read_csv_with_dict(csv_path):\n    \"\"\"Читает CSV файл с использованием DictReader\"\"\"\n    data = []\n    \n    try:\n        with open(csv_path, 'r', encoding='utf-8') as file:\n            reader = csv.DictReader(file)\n            for row in reader:\n                data.append(row)\n            print(f\"Прочитано {len(data)} записей из {csv_path}\")\n    except FileNotFoundError:\n        print(f\"CSV файл {csv_path} не найден\")\n    \n    return data\n\ndef write_csv_with_dict(data, csv_path, fieldnames):\n    \"\"\"Записывает данные в CSV файл с использованием DictWriter\"\"\"\n    with open(csv_path, 'w', encoding='utf-8', newline='') as file:\n        writer = csv.DictWriter(file, fieldnames=fieldnames)\n        writer.writeheader()\n        writer.writerows(data)\n        print(f\"Записано {len(data)} записей в {csv_path}\")\n\n# Пример использования\nusers = [\n    {'name': 'Анна', 'age': '25', 'city': 'Москва'},\n    {'name': 'Борис', 'age': '30', 'city': 'Санкт-Петербург'}\n]\n\nwrite_csv_with_dict(users, 'users.csv', ['name', 'age', 'city'])\nloaded_users = read_csv_with_dict('users.csv')\nprint(loaded_users)\n```\n\n⚠️ **Важные особенности**:\n- **Автоматическое закрытие** — файл закрывается при выходе из блока `with`\n- **Обработка исключений** — файл закрывается даже при возникновении ошибок\n- **Вложенность** — можно использовать несколько `with` блоков\n- **Читаемость** — код становится более понятным и структурированным\n\n🎯 **Когда использовать `with`**:\n- **Всегда** при работе с файлами\n- **Всегда** при работе с сетевыми подключениями\n- **Всегда** при работе с базами данных\n- **Всегда** при работе с любыми внешними ресурсами\n\n💡 **Совет**: Если вы не используете `with` для работы с файлами — вы делаете это неправильно!",
    "title": "Контекстный менеджер with"
}